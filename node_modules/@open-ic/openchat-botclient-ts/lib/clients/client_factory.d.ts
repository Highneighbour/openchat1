import type { ActionScope, BotClientConfig } from "../domain";
import { Permissions } from "../domain/permissions";
import { BotClient } from "./bot_client";
export declare function isMainnet(icUrl: string): boolean;
export declare class BotClientFactory {
    #private;
    constructor(env: BotClientConfig);
    get env(): BotClientConfig;
    createClientInAutonomouseContext(scope: ActionScope, apiGateway: string, autonomousPermissions?: Permissions): BotClient;
    createClientFromCommandJwt(token: string): BotClient;
}
