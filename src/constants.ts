/**
 * OpenChat client identifier
 * This should be added to the Clients enum in ElizaOS core in the future
 */
export const Clients = {
  OPENCHAT: "openchat" as const,
};

export const DEFAULT_PORT = 3000;
export const DEFAULT_IC_HOST = "https://icp-api.io";
